[project]
name = "ticker-cache-mcp"
version = "0.0.1"
description = "MCP server for stock ticker data cache"
requires-python = ">=3.11"
dependencies = ["mcp", "pydantic"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
